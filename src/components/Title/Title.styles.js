import { StyleSheet } from 'react-native';
import { colors } from '../../config';

export default StyleSheet.create({
  text: {
    textAlign: 'center',
    color: colors.yellow,
    fontWeight: '900',
    fontSize: 30,
  },
});
